{
  "kind": "collectionType",
  "collectionName": "comments",
  "info": {
    "singularName": "comment",
    "pluralName": "comments",
    "displayName": "Comment",
    "description": ""
  },
  "options": {
    "draftAndPublish": false
  },
  "attributes": {
    "comment_full_name": {
      "type": "string",
      "required": true
    },
    "comment_body": {
      "type": "text",
      "required": true
    },
    "comment_subject": {
      "type": "string",
      "required": true
    },
    "product": {
      "type": "relation",
      "relation": "manyToOne",
      "target": "api::product.product",
      "inversedBy": "comments"
    },
    "comment_status": {
      "type": "enumeration",
      "enum": [
        "pending",
        "publish",
        "reject"
      ],
      "default": "pending"
    }
  }
}
